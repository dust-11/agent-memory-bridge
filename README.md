# agent-memory-bridge
Tiered memory system for AI agents. Three layers: conversation history (real-time compressed), deep memory (permanent, semantic-search injected), shallow memory (search-only index). SQLite + Node.js bridge. Cross-agent shared (Hermes/OpenClaw), fully local, no vector DB needed.
